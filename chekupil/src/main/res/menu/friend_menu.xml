<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/friend_menu_group_one"
           android:visible="false">
        <item
                android:id="@+id/friend_menu_edit"
                android:orderInCategory="10"
                android:icon="@drawable/ic_edit"
                android:title="@string/friend_popup_menu_edit"
                app:showAsAction="ifRoom|withText"/>
    </group>
    <group android:id="@+id/friend_menu_group_many"
           android:visible="false">
        <item
                android:id="@+id/friend_menu_delete"
                android:icon="@drawable/ic_delete"
                android:orderInCategory="20"
                android:title="@string/friend_popup_menu_delete"
                app:showAsAction="ifRoom|withText"/>
    </group>
</menu>